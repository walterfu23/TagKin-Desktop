# TagKin desktop client (D1 Auth & Account)
# Copy to .env (never commit the real file — R8).
# Only the publishable key belongs here; never put CLERK_SECRET_KEY or
# provider keys in this client.

CLERK_PUBLISHABLE_KEY=
TAGKIN_API_URL=http://localhost:8787
