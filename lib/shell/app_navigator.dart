import 'package:flutter/material.dart';

/// Root [MaterialApp] navigator — dirty leave prompts and other root dialogs.
final GlobalKey<NavigatorState> tagkinRootNavigatorKey =
    GlobalKey<NavigatorState>();
